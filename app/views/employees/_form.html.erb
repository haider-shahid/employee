
  <% if @employee.errors.any? %>
<div class="row">
  <div class = "col-md-6 col-md-offset-3">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@employee.errors.count, "error") %> prohibited this employee from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
          <% @employee.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
  <% end %>

  <%= form_for(@employee ,:html => {class: "form-horizontal" ,role: "form"}) do |form| %>
  <div class="form-group">
    <%= form.label :usname , class:"col-sm-2 control-label col-sm-offset-1" %>
    <div class = "col-sm-6">
      <%= form.text_field :usname , class:"form-control",placeholder:"Enter User Name"%>
    </div>
  </div>

  <div class="form-group">
    <%= form.label :password,class:"col-sm-2 control-label col-sm-offset-1" %>
    <div class="col-sm-6">
      <%= form.text_field :password, class:"form-control", placeholder:"Enter Pasword here" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-3 col-sm-6">
      <%= form.submit class:"btn btn-primary" %>
    </div>
  </div>
<% end %>
